services:
  music-player-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: music-player-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always